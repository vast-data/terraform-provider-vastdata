---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "vastdata_kerberos Resource - vastdata"
subcategory: ""
description: |-
  
---

# vastdata_kerberos (Resource)



## Example Usage

```terraform
# Basic Kerberos configuration
resource "vastdata_kerberos" "basic_kerberos" {
  realm              = "EXAMPLE.COM"
  service_principals = ["nfs/vastcluster.example.com"]
  kdc                = ["kdc.example.com"]
  kadmin_servers     = ["kadmin.example.com"]
  enabled            = true
}

# Advanced Kerberos configuration with multiple service principals
resource "vastdata_kerberos" "advanced_kerberos" {
  realm = "CORP.EXAMPLE.COM"
  service_principals = [
    "nfs/vastcluster.corp.example.com",
    "cifs/vastcluster.corp.example.com",
    "http/vastcluster.corp.example.com"
  ]
  kdc = [
    "kdc1.corp.example.com",
    "kdc2.corp.example.com"
  ]
  kadmin_servers = [
    "kadmin1.corp.example.com",
    "kadmin2.corp.example.com"
  ]
  enabled = true
}

# Kerberos configuration with IP addresses
resource "vastdata_kerberos" "ip_based_kerberos" {
  realm              = "VASTDB.LOCAL"
  service_principals = ["nfs/vastdb"]
  kdc                = ["10.27.14.107"]
  kadmin_servers     = ["10.27.14.107"]
  enabled            = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `kadmin_servers` (Set of String)
- `kdc` (Set of String)
- `realm` (String)
- `service_principals` (Set of String)

### Optional

- `enabled` (Boolean)

### Read-Only

- `guid` (String)
- `id` (Number) The ID of this resource.
- `state` (Attributes) (see [below for nested schema](#nestedatt--state))

<a id="nestedatt--state"></a>
### Nested Schema for `state`

Read-Only:

- `kadmin_servers` (Attributes Set) (see [below for nested schema](#nestedatt--state--kadmin_servers))
- `kdc` (Attributes Set) (see [below for nested schema](#nestedatt--state--kdc))
- `service_principals` (Attributes Set) (see [below for nested schema](#nestedatt--state--service_principals))

<a id="nestedatt--state--kadmin_servers"></a>
### Nested Schema for `state.kadmin_servers`

Read-Only:

- `location` (String)
- `message` (String)
- `status` (String)


<a id="nestedatt--state--kdc"></a>
### Nested Schema for `state.kdc`

Read-Only:

- `location` (String)
- `message` (String)
- `status` (String)


<a id="nestedatt--state--service_principals"></a>
### Nested Schema for `state.service_principals`

Read-Only:

- `message` (String)
- `name` (String)
- `status` (String)
